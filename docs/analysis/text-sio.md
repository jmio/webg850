# TEXT モードの SIO（Save / Load）

`TEXT` キー → `Sio` → `Save` / `Load` で、テキストエディタの内容を外部へ出し入れできる。
本ドキュメントはその経路を実測で確かめた記録である。

**これは実機がどうなっているかの記録であり、エミュレータへの実装は
[../emulator/](../emulator/) に分けてある。**

## 結論: ハードウェア UART は使わない

名前が「SIO」で `Format` 画面にボーレートやパリティの設定があるため、
[11pin-uart.md](11pin-uart.md) に書かれたハードウェア UART（`0x70`-`0x74`）を
使っていると推測していたが、**これは誤りだった。**

実測すると、`Save` の実行中に UART のポートは **1 回も**触られない。
出力は `BSAVE` と同じ **ポート `0x18` の bit7（XOUT）をソフトウェアで叩く**方式で、
違いは変調方式だけである。

| | 変調 | 出力 | 入力 |
|:--|:--|:--|:--|
| `BSAVE` / `BLOAD` | PWM（H 期間の長さでビットを表す）| `0x18` bit7 | `0x1F` bit2 |
| TEXT の SIO | **調歩同期（非同期シリアル）** | `0x18` bit7 | `0x1F` bit2 |
| RS-232C | ハードウェア UART | `0x72` | `0x72` |

`0x60`（11pin の動作モード）も SIO では書かれない。`BSAVE` は書く。

## Format 画面の設定項目

`TEXT` → `Sio` → `Format` で表示される。工場出荷時の値は次のとおり。

| 項目 | 既定値 |
|:--|:--|
| baud rate | 1200 |
| data bit | 8 |
| stop bit | 1 |
| parity | none |
| end of line | CR LF |
| end of file | 1A |

カーソルは `←` `→` `↑` `↓` で移動し、`↵` で確定する。

## 実測（2026-08-30）

`z80inport` / `z80outport` をフックしてポートアクセスを記録し、
テキストエディタに `10 ABCDE` の 1 行を入れた状態で `Sio` → `Save` を実行した。

### アクセスされたポート

送信中に触られる 11pin 関連のポートは次の 3 つだけだった。

| ポート | 向き | 内容 |
|:--|:--|:--|
| `0x15` | 出力 | 送信開始時に **`0x80` を 1 回**だけ書く |
| `0x18` | 出力 | bit1 を立てた状態で bit7 にデータを乗せる |
| `0x1F` | 入力 | 送信可否の待ち合わせ（後述）|

**`0x60` および `0x70`-`0x74` へのアクセスは 0 回。** これが UART を使っていない根拠。
なお `0x19` / `0x69`（ROM バンク）と `0x40` / `0x41`（LCD）は常時アクセスされるので
SIO とは無関係である。

`0x15` は [11pin-uart.md](11pin-uart.md) で「Xin 入力イネーブル。どのビットが
何を有効にするかは不明」としていたポートで、SIO 送信の開始時に `0x80` が書かれる。
`Save`（出力）でも書かれるのは、後述の待ち合わせ入力を有効にするためと思われる。

### フレーム形式

`0x18` への書き込みのうち **bit1 が 1 のとき**が SIO の送出で、
そのときの bit7 がデータビットになる。**bit7 は論理値の反転**（bit7 = 1 でデータ 0）。

```
アイドル/ストップ = 1
スタートビット    = 0
データ 8 ビット   = LSB ファースト
```

`Format` 画面の既定値（データ 8 ビット、ストップ 1 ビット、パリティなし）と一致する。

### ビット周期とボーレート

bit7 の変化点の間隔を Z80 のステート数で測ったところ、**6733 ステート**で一定だった。

| クロック | ビット周期 | ボーレート |
|:--|--:|--:|
| エミュレータ（9000 kHz）| 748.1 usec | 1337 baud |
| 実機（8 MHz と仮定）| 841.6 usec | 1188 baud |

実機のクロックで計算すると `Format` 画面の **1200 baud と 1% 以内で一致**する。
この 9/8 のずれは `BSAVE` の PWM で観測したものと同じで、
エミュレータのクロックが実機より約 11% 速いことの表れである
（[bsave-signal-format.md](bsave-signal-format.md) 参照）。

ソフトウェアでビットを叩いている以上、ボーレートは CPU クロックに直結する。
`Format` で 9600 baud を選べば周期は 1/8 になるはずだが未確認。

### 送信前の待ち合わせ

**ROM は `0x1F` の bit1 が 1 になるまで送信を始めない。**

bit1 が 0 のままだと画面は `--- SENDING ---` を表示したまま止まり、
`0x1F` をひたすら読み続ける。実測では 3800 万ステート（実機で約 4.8 秒）の間に
**126 万回**読んでいた。約 30 ステートに 1 回の密なポーリングである。

bit1 を 1 にすると送信が始まり、`0x18` への書き込みが現れる。
受け側の準備完了（CTS 相当）を見ていると考えられるが、
11pin のどのピンに対応するかは未確認。

### 復号の確認

エミュレータ側で bit1 を 1 にして上記の規則で復号したところ、
エディタに入れた内容がそのまま得られた。

```
10 ABCDE\n
```

## 未確認事項

- `0x1F` bit1 が 11pin のどのピンに対応するか
- `Load` 側の入力ビット（`0x1F` bit2 を使うのか、bit1 と兼用なのか）
- `Format` でボーレートを変えたときのビット周期
- `end of line` / `end of file` の設定が送出データにどう反映されるか
- `0x15` の `0x80` が具体的に何を有効にしているか
- `0x18` bit1 の役割（SIO のイネーブルなのか、別の意味があるのか）
